# Sample-Calculator
A lightweight tool to compute the required number of samples for robust convex optimization based on the scenario approach. The application is deployed as an interactive web app using Streamlit, enabling quick and intuitive evaluation of sample complexity for different parameters.
